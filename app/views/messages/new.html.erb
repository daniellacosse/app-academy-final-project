<%= render partial: "users/content", locals: {user: current_user} %>
<%= render partial: "users/info", locals: {user: current_user} %>
<section class="form main-content center">
  <h1 class="form-header">
    Write Message to <%= @user.username %>
  </h1>

  <%= render 'form', locals: {message: @message} %>
</section>